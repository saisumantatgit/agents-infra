# Red-Team Round 3 — Agent-Assure grounding moat

**Date:** 2026-08-30
**Target:** `scripts/ground_check.py` @ branch `agent-assure-calibration-run`
**Store:** `tests/red_team_moat/fixtures/store.jsonl` (S1 Redis benchmark, S2 PostgreSQL benchmark; both `verbatim`)
**Command:** `uv run python scripts/ground_check.py --draft D.md --store tests/red_team_moat/fixtures/store.jsonl --json`
**Operating point:** `lex_tau = 0.71` (deployed today, OI-CAL-01), score threshold 90, ADR-005 empty-appendix cap.

**Goal:** find drafts where `gate == "PASS"` (exit 0) while the draft carries a fabricated or
unsupported claim. Any such draft is an **Error-B / release blocker**.

**Fixes under test (all landed 2026-08-30):** OI-MOAT-07 (`_is_non_claim` verbless-fragment
content-token floor), OI-MOAT-03 (`t1_verbatim` residual-coverage), OI-MOAT-05
(`ground_relational` + `_relation_asserted` predicate check).

## Results table

<!-- RESULTS -->

## Findings

<!-- FINDINGS -->
