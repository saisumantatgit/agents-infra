# Red-Team Round 4 — Agent-Assure grounding moat

**Date:** 2026-08-30
**Target:** `scripts/ground_check.py` @ branch `agent-assure-calibration-run` (post-R3 fixes)
**Store:** `tests/red_team_moat/fixtures/store.jsonl` (S1 Redis, S2 PostgreSQL; both `verbatim`)
**Command:** `PYTHONPATH=. uv run python scripts/ground_check.py --draft D.md --store tests/red_team_moat/fixtures/store.jsonl --json`
**Operating point:** `_LEX_TAU_DEFAULT = 0.71` (ground_check.py:738), score threshold 90, ADR-005 empty-appendix cap.

**Goal:** drafts where `gate == "PASS"` (exit 0) while carrying a fabricated or unsupported claim = Error-B.

**Fixes under test (landed 2026-08-30, hours old):** RT3-01 (`_is_non_claim` all-proper-noun rule),
RT3-02 (header `_has_finite_verb` test), RT3-03 (`t1_verbatim` per-source coverage),
RT3-04 (`check_absence` content-contradiction + negation cue), RT3-05 (`_numeric_context`
rate-scoped quantity carry-through).

*(status: IN PROGRESS — table and findings appended as drafts run)*

## Results table

| Draft | Family | Gate | Score | Verdicts | Wrongful PASS |
|---|---|---|---|---|---|
