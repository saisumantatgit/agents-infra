[project]
name = "agent-assure"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["syntok", "pyyaml"]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
