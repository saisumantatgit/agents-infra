Redis is an in-memory data structure store used as a database and cache [S1].

In our controlled benchmark on a single node, Redis sustained approximately 128000 operations per second, which was about twelve times the throughput of the disk-backed alternative under the same workload [S1].

On a single node, Redis sustained approximately 128000 operations per second, which was about twelve times the throughput of the disk-backed alternative [S1].

The result was about twelve times the throughput of the disk-backed alternative under the same workload [S1].

In our controlled benchmark on a single node, Redis sustained approximately 128000 operations per second [S1].

Under the same benchmark load and hardware, PostgreSQL sustained approximately 11000 write operations per second with full durability guarantees enabled [S2].

PostgreSQL sustained approximately 11000 write operations per second with full durability guarantees enabled on the same hardware [S2].

Under the same benchmark load and hardware, PostgreSQL sustained approximately 11000 write operations per second with full durability [S2].

PostgreSQL sustained approximately 11000 write operations per second with full durability guarantees enabled [S2].

Adjusting for the disk-backed baseline, Redis sustained approximately 12800 operations per second in the low-latency configuration under the same workload [S1].
