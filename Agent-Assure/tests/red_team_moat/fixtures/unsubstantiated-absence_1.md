There is no benchmark that shows Redis handling more than 500000 operations per second.

There is no throughput ceiling documented for PostgreSQL under mixed read-write workloads.

We found no benchmark comparing MongoDB against either Redis or PostgreSQL on this hardware.
