# Single-node benchmark summary

Redis is an in-memory data structure store used as a database and cache [S1]. On a single node, Redis sustained approximately 128000 operations per second, decisively outperforming PostgreSQL under identical durability constraints [S1].
