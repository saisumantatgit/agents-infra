# Overnight queue — 2026-09-13, HARD STOP 01:30 IST

Cron-advanced; durable, the cron dies with the session. Armed 00:08 IST.

## The rule for tonight: DECISION-INDEPENDENT WORK ONLY

**The product call is Sai's and is NOT made:** ship provenance-only, or fund
entailment. Nothing tonight may presume the answer. So no taxonomy split, no
`--experimental-entailment` flag, no J-21 (sentence-bounded complements) — all
of that is wasted if he funds entailment, or premature if he doesn't.

Everything below is valuable **under either answer**, because provenance ships
in both futures and the spec names it as the identity.

## Parked — no ruling, do NOT assume one

The product call · J-22 whitelist verbs · q25 gold adjudication · OI-MOAT-31 ·
J-15 · D-07 · `docs/consulting/` · applying bindings · gross_wages · anything
outward-facing. **Do NOT build T3. Do NOT change any gold label.**

## Queue

- [x] **1. DONE 00:12 — store gaps are SAFE for ordinary claims and FAIL-OPEN
      for absence claims, demonstrated: the certified q13 absence reads PASS
      100.0 on the captured store and FAIL once one uncaptured WebSearch result
      is added. Also found auto mode instructs file reads via Bash, which the
      hook never sees. Nothing changed — every remedy is hook registration,
      Escalation #4 (D-37).** ~~original:~~ **§7.5 audit — the spec's own named #1 risk, never closed.** The
      founding spec: *"The gate is only as truthful as the EvidenceStore … Get
      this wrong and the score is theater. This is the top implementation-plan
      risk to close."* Enumerate every path by which content can reach a
      drafting model in a Claude Code session (native tools, MCP tools,
      pasted text, prior-turn content, subagent returns, file reads, bash
      output) and check each against what `hooks/hooks.json` +
      `capture_hook.py` actually capture. **Output: a coverage table with each
      path marked CAPTURED / NOT CAPTURED / PARTIAL**, and for each gap the
      failure it causes (a genuinely grounded claim falsely flagged
      UNVERIFIED_CITATION, or a route around the hook). Read and analyse only;
      any fix is a SEPARATE decision recorded as D-37+ and must be fail-closed.
      Report: `Agent-Assure/docs/reports/SPEC-7.5-STORE-COMPLETENESS-2026-09-13.md`. ~0.3M
- [ ] **2. Round 9 — PROVENANCE ONLY.** A red team scoped to the closed class:
      fabricated citations, unresolvable ids, id-form tricks (unicode,
      brackets, case, zero-width, lookalike ids), summary-vs-verbatim
      spoofing, citation placement, store tampering, duplicate ids, a claim
      that cites a real source for text that source does not contain. **This
      round is winnable** because the class is closed — which is exactly why
      it is worth running before a launch that stakes the product on it.
      Entailment findings are OUT OF SCOPE; record and discard. ~0.5M
- [ ] **3. Close — at 01:30 IST via `/sg close` (Sai, 00:30), regardless of 1 and 2.** Register, logbook,
      RESUME-HERE, push, CronDelete. A half-finished round 9 with no handoff is
      worse than no round 9.

## Stop conditions

- **The close is its own job at 01:30 (`/sg close`). Earlier ticks must not run it.**
  Hard stop is 01:30; the close needs its ten minutes.
- Any PASS-enabling change → tripwire, do not fix.
- A gold label needs changing → STOP, Sai's.
